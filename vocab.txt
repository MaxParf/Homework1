🔍 Поиск элементов в DOM:

document.querySelector() - Вернет из DOM первый элемент с заданным селектором

document.querySelectorAll() - Вернет из DOM все виде псевдо массива

element.getElementById() - Находит первый по ID
 
element.getElementsByClassName() 

element.getElementsByTagName() 

🔧 Работа с содержимым:

element.textContent - Получает или задает текст внутри элемента

element.innerHTML - Получает или задает HTML-содержимое элемента

element.value – Работает с полями ввода (input, textarea, select)

element.setAttribute() – Устанавливает атрибут (class, id, data-* и т. д.)

element.getAttribute() – Получает значение атрибута

element.classList.add() / remove() / toggle() – Управляет классами элемента (добавить, удалить, переключить)

🧱 Работа со структурой:

document.createElement() – Создает новый HTML-элемент

element.appendChild() – Добавляет элемент в конец другого элемента

element.remove() – Удаляет элемент из DOM

element.parentNode – Возвращает родительский элемент

element.children – Возвращает дочерние элементы

element.insertBefore() / after() / before() – Вставляет элемент до, после или перед указанным.

🎯 Обработка событий:

element.addEventListener('click', ...) - Слушает клик

event.target – Элемент, на котором произошло событие

event.preventDefault() – Отменяет стандартное поведение (например, переход по ссылке)

⏰ Время:

setTimeout(callback, delay) – Выполняет функцию один раз через указанное время (мс)

setInterval(callback, delay) – Выполняет функцию периодически с заданным интервалом
